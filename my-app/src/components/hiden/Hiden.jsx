import React, { useState } from 'react'

const Hiden = ({ typePassword, setTypePassword }) => {
    const [typePassword, setTypePassword] = useState("password")

    return (
        <div>Hiden</div>
    )
}

export default Hiden